// Einzige Quelle der Wahrheit fuer den GitHub-Pages-basePath.
// Wird von next.config.mjs, playwright.config.ts und tests/e2e/app.spec.ts importiert,
// damit der Pfad nicht mehrfach dupliziert werden muss (S2, Sprint Sonnet 31.07.2026).
export const GITHUB_PAGES_BASE_PATH = "/kleinkunst-veranstalter-dashboard";
